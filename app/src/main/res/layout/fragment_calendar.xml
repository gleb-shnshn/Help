<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.prolificinteractive.materialcalendarview.MaterialCalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:mcv_dateTextAppearance="@style/AppTheme.CalendarDay"
        app:mcv_headerTextAppearance="@style/AppTheme.CalendarHeader"
        app:mcv_selectionColor="#771976D2"
        app:mcv_showOtherDates="all"
        app:mcv_weekDayTextAppearance="@style/AppTheme.CalendarHeader" />

    <Button
        android:id="@+id/toDay"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="30dp"
        android:background="@color/colorPrimary"
        android:drawableStart="@drawable/ic_time"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:text="Перейти к выбранному дню"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>